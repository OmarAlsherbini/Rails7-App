<%= form_with(model: calendar_app, class: "contents") do |form| %>
  <% if calendar_app.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(calendar_app.errors.count, "error") %> prohibited this calendar_app from being saved:</h2>

      <ul>
        <% calendar_app.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  <span>
    <%= form.label "Months forward & backward:", style: "mr-30 text-orange-600" %>
    <%= form.number_field :n_mon_span, style: "ml-30"  %>
  </span>
  </div>

  <div>
  <span>
    <%= form.label "Years forward & backward:", style: "mr-30 text-orange-600" %>
    <%= form.number_field :n_yr_span, style: "ml-30"  %>
  </span>
  </div>

  <div>
  <%= form.label :include_current_month_in_past, style: "display: block" %>
  <%= form.check_box :include_current_month_in_past %>
  
    
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
